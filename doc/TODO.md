每项完成后进行标记

# Bug 修复 ✅


# 新功能 ✅
- [x] 分类页面/categories改版，分为两列，左侧为分类的树形结构，右侧为该分类下的文章列表，树形结构可折叠或展开，要有文件夹图标；
- [x] 侧边栏的分类只显示顶层的分类，子分类不显示；
- [x] 从文章或其他地方跳转到分类，可以在树形结构中找到该分类，并自动加载该分类下的文章列表；
- [x] 实现单用户模式，不允许注册多个，第一个用户需要一个控制台的密码
- [x] 页面增加左侧边栏，将博主信息移到左侧边栏
- [x] header左侧在低分辨率情况下也展示一个博主头像并收起左侧边栏，逻辑同右侧边栏
- [x] 博主下边增加一个广告位；
- [x] 分类和标签自动上移
- [x] 个人信息增加微信二维码，个人中心和后台可上传，在当前联系方式下方展示，显示：添加博主微信，然后显示二维码图片
- [x] 如果设置了微信二维码,则显示一个微信的图标,鼠标放上去显示二维码的图片
- [x] 默认主题改为深色
- [x] APP header 的分类归档标签放到搜索框的右侧，去掉首页（因为可以直接点博客标题）；
- [x] 增加一个关于按钮，然后显示个人的一个介绍，可以通过后台管理通过markdown来进行配置，前端跳转about页面，后端用单独的接口查询具体内容。
- [x] 文章编辑页面支持定期自动保存草稿，默认2分钟，可在页面临时设置，保存后保持当前页面不影响当前输入，点击保存草稿的逻辑也是一样不会退出编辑页面。
- [x] 文章访问统计：新增文章统计表，记录文章id、访问者id、访问日期（天）。
- [x] 逻辑：每篇文章每个人只统计一次，统计数据启动是放入缓存，每5分钟将有变化的更新到数据文章表中。
- [x] 通过后端的slug查询接口来统计访问次数。
- [x] 页面的文章列表、热门文章和详情页面的统计数据都查询缓存。
- [x] 优化文章搜索，输入后就进行搜索，而不是回车后才搜索，直接显示文章标题，点击后直接进入文章详情页。跳转slug不是id
- [x] 高亮搜索匹配的文字，如果是内容匹配则每个选项显示第二行，显示匹配内容附近的文字，也要高亮匹配文字
- [x] 行内代码复制按钮
- [x] 前端文章内的图片支持全屏查看