
# 博客前端设计文档

## 1. 设计原则

- 页面简洁美观，符合 Material Design 设计规范
- 适当添加动画效果，提升用户体验
- 响应式设计，兼容不同屏幕大小（PC、平板、手机）
- 支持主题切换（亮色/暗色模式）
- 编辑使用 Markdown 格式，图片使用外部链接

## 2. 技术栈

### 2.1 核心框架
- **Vue 3** - 渐进式 JavaScript 框架
- **Vite** - 下一代前端构建工具
- **Vue Router 4** - 官方路由管理器
- **Pinia** - Vue 3 状态管理库

### 2.2 UI 框架
- **Element Plus** - 基于 Vue 3 的组件库，符合 Material Design 风格

### 2.3 功能库
- **axios** - HTTP 请求库
- **@vueuse/core** - Vue Composition API 工具集
- **markdown-it** - Markdown 解析器
- **highlight.js** - 代码高亮
- **dayjs** - 日期处理库

## 3. 页面设计

### 3.1 公共布局

```
┌─────────────────────────────────────────────────────────┐
│  Header（导航栏）                                          │
│  - Logo / 站点名称                                        │
│  - 导航菜单（首页、归档、分类、标签）                        │
│  - 搜索框                                                 │
│  - 用户信息/登录按钮                                       │
│  - 主题切换按钮                                            │
├─────────────────────────────────────────────────────────┤
│                                        │                  │
│  Main Content                          │  Sidebar         │
│  - 文章列表/详情                        │  - 分类列表       │
│  - 归档页面                             │  - 标签云         │
│  - 管理后台                             │  - 热门文章       │
│                                        │  - 最新评论       │
│                                        │                  │
├─────────────────────────────────────────────────────────┤
│  Footer（页脚）                                           │
│  - 版权信息                                               │
│  - 备案号                                                 │
│  - 社交链接                                               │
└─────────────────────────────────────────────────────────┘
```

### 3.2 页面列表

| 页面 | 路由 | 描述 |
|------|------|------|
| 首页 | `/` | 文章列表，支持分页、搜索、筛选 |
| 文章详情 | `/post/:id` | 文章内容、评论、相关文章推荐 |
| 归档 | `/archives` | 按年月归档的文章列表 |
| 分类列表 | `/categories` | 所有分类展示 |
| 分类文章 | `/category/:id` | 指定分类下的文章 |
| 标签列表 | `/tags` | 标签云展示 |
| 标签文章 | `/tag/:id` | 指定标签下的文章 |
| 搜索结果 | `/search` | 关键词搜索结果 |
| 登录 | `/login` | 用户登录 |
| 注册 | `/register` | 用户注册 |
| 个人中心 | `/profile` | 用户信息管理 |
| 后台首页 | `/admin` | 管理后台仪表盘 |
| 文章管理 | `/admin/posts` | 文章增删改查 |
| 写文章 | `/admin/posts/write` | Markdown 编辑器 |
| 编辑文章 | `/admin/posts/edit/:id` | 编辑已有文章 |
| 分类管理 | `/admin/categories` | 分类管理 |
| 标签管理 | `/admin/tags` | 标签管理 |
| 评论管理 | `/admin/comments` | 评论审核与管理 |
| 用户管理 | `/admin/users` | 用户管理（管理员） |

## 4. 组件设计

### 4.1 布局组件
- `AppLayout` - 主布局容器
- `AppHeader` - 顶部导航栏
- `AppSidebar` - 侧边栏
- `AppFooter` - 页脚
- `AdminLayout` - 后台管理布局

### 4.2 业务组件
- `PostCard` - 文章卡片
- `PostList` - 文章列表
- `PostDetail` - 文章详情
- `CommentList` - 评论列表
- `CommentForm` - 评论表单（登录用户直接显示输入框，匿名用户使用弹窗）
- `CategoryCard` - 分类卡片
- `TagCloud` - 标签云
- `ArchiveTimeline` - 归档时间线
- `SearchBox` - 搜索框
- `Pagination` - 分页组件

### 4.3 表单组件
- `LoginForm` - 登录表单
- `RegisterForm` - 注册表单
- `PostForm` - 文章编辑表单
- `CategoryForm` - 分类表单
- `TagForm` - 标签表单

## 5. API 接口对接

### 5.1 请求配置
```javascript
// axios 实例配置
const api = axios.create({
  baseURL: '/api',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器 - 添加 Token
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器 - 统一错误处理
api.interceptors.response.use(
  response => response.data,
  error => {
    if (error.response?.status === 401) {
      // 跳转登录页
      router.push('/login')
    }
    return Promise.reject(error)
  }
)
```

### 5.2 接口模块
```javascript
// 认证模块
auth: {
  login: POST /auth/login,
  register: POST /auth/register,
  logout: POST /auth/logout
}

// 用户模块
user: {
  profile: GET /user/{id},
  update: PUT /user,
  list: GET /user,
  create: POST /user,
  delete: DELETE /user
}

// 文章模块
post: {
  list: GET /post,
  detail: GET /post/{id},
  detailWithContent: GET /post/{id}/detail,
  create: POST /post,
  update: PUT /post,
  delete: DELETE /post,
  search: GET /post/search,
  archives: GET /post/archives,
  drafts: GET /post/drafts,
  saveDraft: POST /post/draft,
  publish: POST /post/publish
}

// 分类模块
category: {
  list: GET /category,
  detail: GET /category/{id},
  create: POST /category,
  update: PUT /category,
  delete: DELETE /category/{id}
}

// 标签模块
tag: {
  list: GET /tag,
  detail: GET /tag/{id},
  create: POST /tag,
  update: PUT /tag,
  delete: DELETE /tag/{id}
}

// 评论模块
comment: {
  list: GET /comment,
  byPost: GET /comment/post/{postId},
  create: POST /comment,
  update: PUT /comment,
  delete: DELETE /comment,
  audit: PUT /comment/audit,
  pending: GET /comment/pending
}
```

## 6. 状态管理

### 6.1 Store 模块
```javascript
// stores/user.js - 用户状态
{
  state: {
    user: null,
    token: null,
    isLoggedIn: false
  },
  actions: {
    login, logout, register, fetchProfile
  }
}

// stores/app.js - 应用状态
{
  state: {
    theme: 'light',
    sidebarCollapsed: false
  },
  actions: {
    toggleTheme, toggleSidebar
  }
}

// stores/post.js - 文章状态
{
  state: {
    posts: [],
    currentPost: null,
    archives: []
  }
}
```

## 8. Cookie 管理

### 8.1 匿名用户评论信息
为提升用户体验，匿名用户的评论信息会保存到 Cookie 中，下次访问时自动填充：

```javascript
// utils/cookie.js - Cookie 工具函数
export function setCookie(name, value, days = 7) {
  const date = new Date()
  date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000))
  const expires = `expires=${date.toUTCString()}`
  document.cookie = `${name}=${encodeURIComponent(value)}; ${expires}; path=/`
}

export function getCookie(name) {
  const nameEQ = `${name}=`
  const ca = document.cookie.split(';')
  for (let i = 0; i < ca.length; i++) {
    let c = ca[i]
    while (c.charAt(0) === ' ') c = c.substring(1, c.length)
    if (c.indexOf(nameEQ) === 0) {
      return decodeURIComponent(c.substring(nameEQ.length))
    }
  }
  return null
}

// 保存的 Cookie 名称
- comment_author_name: 评论者昵称（7 天有效期）
- comment_author_email: 评论者邮箱（7 天有效期）
```

## 9. 主题设计

### 9.1 颜色方案
```scss
// 亮色主题
--primary-color: #409eff;      // 主色调
--success-color: #67c23a;      // 成功
--warning-color: #e6a23c;      // 警告
--danger-color: #f56c6c;       // 危险
--info-color: #909399;         // 信息
--text-primary: #303133;       // 主要文字
--text-regular: #606266;       // 常规文字
--text-secondary: #909399;     // 次要文字
--border-color: #dcdfe6;       // 边框颜色
--background: #ffffff;         // 背景色

// 暗色主题
--primary-color: #409eff;
--text-primary: #e5eaf3;
--text-regular: #cfd3dc;
--text-secondary: #a3a6ad;
--border-color: #4c4d4f;
--background: #141414;
```

### 9.2 响应式断点
```scss
$breakpoints: (
  'xs': 0,      // 手机
  'sm': 576px,  // 大手机
  'md': 768px,  // 平板
  'lg': 992px,  // 小桌面
  'xl': 1200px, // 大桌面
  'xxl': 1400px // 超大桌面
);
```

## 10. 动画效果

### 10.1 页面过渡
```scss
// 路由切换动画
.fade-slide-enter-active,
.fade-slide-leave-active {
  transition: all 0.3s ease;
}
.fade-slide-enter-from {  opacity: 0;
  transform: translateY(20px);
}
.fade-slide-leave-to {
  opacity: 0;
  transform: translateY(-20px);
}
```

### 10.2 组件动画
- 列表项渐入效果
- 卡片悬停效果
- 按钮点击反馈
- 加载骨架屏

## 11. 目录结构

```
frontend/
├── public/
│   └── favicon.svg
├── src/
│   ├── api/                # API 接口
│   │   ├── index.js        # axios 实例
│   │   ├── auth.js         # 认证接口
│   │   ├── post.js         # 文章接口
│   │   ├── category.js     # 分类接口
│   │   ├── tag.js          # 标签接口
│   │   └── comment.js      # 评论接口
│   ├── assets/             # 静态资源
│   │   ├── images/
│   │   └── styles/
│   │       ├── variables.scss
│   │       ├── reset.scss
│   │       └── global.scss
│   ├── components/         # 公共组件
│   │   ├── layout/
│   │   │   ├── AppHeader.vue
│   │   │   ├── AppSidebar.vue
│   │   │   └── AppFooter.vue
│   │   ├── post/
│   │   │   ├── PostCard.vue
│   │   │   └── PostList.vue
│   │   └── common/
│   │       ├── Pagination.vue
│   │       └── SearchBox.vue
│   ├── composables/        # 组合式函数
│   │   ├── useAuth.js
│   │   └── useTheme.js
│   ├── router/             # 路由配置
│   │   └── index.js
│   ├── stores/             # 状态管理
│   │   ├── user.js
│   │   ├── app.js
│   │   └── post.js
│   ├── views/              # 页面视图
│   │   ├── home/
│   │   │   └── Index.vue
│   │   ├── post/
│   │   │   ├── Detail.vue
│   │   │   └── Search.vue
│   │   ├── archive/
│   │   │   └── Index.vue
│   │   ├── category/
│   │   │   ├── Index.vue
│   │   │   └── Posts.vue
│   │   ├── tag/
│   │   │   ├── Index.vue
│   │   │   └── Posts.vue
│   │   ├── auth/
│   │   │   ├── Login.vue
│   │   │   └── Register.vue
│   │   ├── profile/
│   │   │   └── Index.vue
│   │   └── admin/
│   │       ├── Index.vue
│   │       ├── posts/
│   │       │   ├── Index.vue
│   │       │   └── Edit.vue
│   │       ├── categories/
│   │       │   └── Index.vue
│   │       ├── tags/
│   │       │   └── Index.vue
│   │       ├── comments/
│   │       │   └── Index.vue
│   │       └── users/
│   │           └── Index.vue
│   ├── utils/              # 工具函数
│   │   ├── storage.js
│   │   ├── format.js
│   │   └── cookie.js       # Cookie 管理工具
│   ├── App.vue
│   └── main.js
├── index.html
├── vite.config.js
└── package.json
```

## 12. 开发计划

### 第一阶段：基础搭建
- [x] 项目初始化
- [ ] 安装依赖
- [ ] 配置路由
- [ ] 配置状态管理
- [ ] 封装 API 请求

### 第二阶段：前台页面
- [x] 主布局组件
- [x] 首页文章列表
- [x] 文章详情页
- [x] 归档页面
- [x] 分类/标签页面
- [x] 搜索功能
- [x] 评论功能（登录用户直接显示输入框，匿名用户弹窗 +Cookie 保存）

### 第三阶段：用户功能
- [ ] 登录/注册页面
- [ ] 个人中心
- [ ] 权限控制

### 第四阶段：后台管理
- [ ] 后台布局
- [ ] 文章管理
- [ ] 分类管理
- [ ] 标签管理
- [ ] 评论管理
- [ ] 用户管理

### 第五阶段：优化完善
- [ ] 响应式适配
- [ ] 主题切换
- [ ] 动画效果
- [ ] 性能优化
